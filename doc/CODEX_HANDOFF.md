# tsune-links 引き継ぎ

- 商品紹介サイト。入口は `index.html`、`css/`、`js/`、`category/`、`product/`。
- 生成元は `../sns/src/afi/html_generator.py` と `../sns/src/afi/tsune_links_manager.py`。生成元を編集する場合は sns の AGENTS.md を先に読む。
- 確認日 2026-09-10。開始時 HEAD `1a6ec5f7`、master、作業ツリーは clean、origin あり。
- このフォルダに旧 CLAUDE.md・専用メモリは未検出。共通メモリの手順は、プロジェクトルート基準で `../doc/CODEX_HANDOFF.md` が存在する場合に読む。
- 開始時は Git 差分・最新履歴を確認する。デプロイや生成スクリプトの実行は、その時の依頼と副作用を確認する。
- 今回は Codex が参照する文書のみ追加。サイト表示・生成処理は変更していない。
